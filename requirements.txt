# Core PyTorch Ecosystem
torch==2.0.1+cu118          # Core framework - match CUDA version (11.8 in this case)
torchvision==0.15.2+cu118   # Image transformations & datasets
timm==0.9.2                 # Pretrained models (EfficientNet-B4/ViT) - critical for architecture
einops==0.6.1               # Tensor reshaping operations (used in Transformer blocks)

# Medical Imaging Processing
nibabel==5.1.0              # NIfTI file I/O (essential for BraTS dataset)
SimpleITK==2.2.1            # Alternative medical image processing
scikit-image==0.21.0        # Image resizing & preprocessing

# Medical Imaging Metrics <--- Added Category
MedPy==0.4.0                # Hausdorff distance, Dice, etc. for medical images <-- Added MedPy

# Training & Optimization
torchmetrics==1.0.3         # Metrics (Dice, IoU) - newer version supports medical segmentation
torchviz==0.0.2             # Computation graph visualization (optional)

# Data Handling & Utilities
numpy==1.23.5               # Array operations
scipy==1.11.1               # Morphological operations (binary dilation/erosion)
tqdm==4.65.0                # Progress bars
matplotlib==3.7.1           # Visualization (training curves, sample predictions)
Pillow==10.0.0              # Image handling
opencv-python-headless==4.7.0.72 # Image processing (headless for server environments)

# Optional Dependencies
# boto3==1.26.154           # AWS S3 access (only needed for cloud storage)
# psutil==5.9.5             # System monitoring (optional resource tracking)
# humanize==4.6.0           # Readable format for metrics (optional)
